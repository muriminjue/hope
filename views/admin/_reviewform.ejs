<div class="form-group">
<label for="title">Book Title</label>
<input required type="text" value="<%= book.title; %>" name="title" id="title" class="form-control" style="width: auto;">
</div>
<div class="form-group" style="width: 30pc;">
<label for="coverImage"> Book Cover Image</label>
<input name= "coverImage" type="file" class="filepond">
</div>
<div class="form-group" style="width: 30pc;">
<label for="bkauthor">Book Author</label>
<input required type="text" value="<%= book.author; %>" name="bkauthor" id="bkauthor" class="form-control" style="width: auto;">
</div>
<div class="form-group">
<label for="description">Description</label>
<textarea required  name="description" id="description" class="form-control" style="width: auto;"><%= book.description; %></textarea>
</div>
<div class="form-group" style="width: 30pc;">
<label for="publisher">Book Publisher</label>
<input required type="text" value="<%= book.publisher; %>" name="publisher" id="publisher" class="form-control" style="width: auto;">
</div>
<div class="form-group" style="width: 30pc;">
<label>Writen by:</label><br>
  <select name="writers[]"  class="mb-2">
    <% authors.forEach(author => { %>
    <% if (author.id === book.writer) { %>
      <option selected label="<%= author.name %> (<%= author.authslug %>)" value="<%= author.id %>">
    <% } else { %>
      <option label="<%= author.name %> (<%= author.authslug %>)" value="<%= author.id %>">
    <% } %>
  <% }); %>
  </select><br>
  <select name="writers[]" id="myDropdown" class="mb-2">
  <% authors.forEach(author => { %>
    <% if (author.id === book.writer) { %>
      <option selected label="<%= author.name %> (<%= author.authslug %>)" value="<%= author.id %>">
    <% } else { %>
      <option label="<%= author.name %> (<%= author.authslug %>)" value="<%= author.id %>">
    <% } %>
  <% }); %>
  </select><br>
  <select name="writers[]" id="myDropdown2" class="mb-2">
  <% authors.forEach(author => { %>
    <% if (author.id === book.writer) { %>
      <option selected label="<%= author.name %> (<%= author.authslug %>)" value="<%= author.id %>">
    <% } else { %>
      <option label="<%= author.name %> (<%= author.authslug %>)" value="<%= author.id %>">
    <% } %>
  <% }); %>
  </select><br>
    <select name="writers[]" id="myDropdown3" >
  <% authors.forEach(author => { %>
    <% if (author.id === book.writer) { %>
      <option selected label="<%= author.name %> (<%= author.authslug %>)" value="<%= author.id %>">
    <% } else { %>
      <option label="<%= author.name %> (<%= author.authslug %>)" value="<%= author.id %>">
    <% } %>
  <% }); %>
  </select>


  </div>
  <script> document.getElementById("myDropdown").selectedIndex = -1;
            document.getElementById("myDropdown2").selectedIndex = -1;
            document.getElementById("myDropdown3").selectedIndex = -1;
  </script>
 <div class="form-group">
<label for="markdown">Markdown</label>
<textarea required  name="markdown" id="markdown" class="form-control"style="width: 50pc;"><%= book.markdown; %></textarea>
</div>
<a href="/admin" class="btn btn-secondary">cancel</a>
<button type="submit" class="btn btn-primary">save</button>